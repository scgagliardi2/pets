namespace Pets.Simulation
{
    /// <summary>See battle-sim-spec.md §5. Only one status applies to a mon at a time.</summary>
    public enum StatusType
    {
        Poisoned,
        Burned,
        Paralyzed,
        Asleep
    }
}
