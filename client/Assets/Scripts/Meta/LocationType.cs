namespace Pets.Meta
{
    /// <summary>The kinds of Location a Region offers (design doc §4's table). See LocationCatalog
    /// for what each one means.</summary>
    public enum LocationType
    {
        Town,
        City,
        Dungeon,
        Cave,
        Forest,
        Sea,
        Plains,
        Desert,
        Mountain
    }
}
