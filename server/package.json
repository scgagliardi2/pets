{
  "name": "pets-server",
  "private": true,
  "version": "0.0.0",
  "description": "Backend for the auto-battler — scaffolded in Phase 0, not wired up until Phase 3 (see PLAN.md).",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no server tests yet (Phase 3+)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
