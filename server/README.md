# server

Node.js/TypeScript backend for the auto-battler. **Scaffolded in Phase 0, not implemented.**

Per PLAN.md §6, this stays empty (structure only) until Phase 3 — the MVP (Phases 0-2) is a
fully offline Unity client with local save. Don't add real routes/services/db code here before
Phase 3 starts.
